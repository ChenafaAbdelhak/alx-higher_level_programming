#!/usr/bin/python3
''' module to check if an instance is of a class type'''


def is_same_class(obj, a_class):
    '''
    return true if obj is an instance of a_class
    otherwise false
    '''

    if type(obj) == a_class:
        return True
    else:
        false
